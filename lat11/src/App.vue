<template>
  <div>
    <the-header></the-header>
    <!-- <TheHeader /> -->
    <button @click="setSelectedComponent('active-goals')">Active Goals</button>
    <button @click="setSelectedComponent('manage-goals')">Manage Goals</button>

    <keep-alive>
      <!-- 
        <keep-alive> -> gunanya adalah untuk membuat component yang di render tidak langsung di remove/replace dengan component baru
                        tetapi di store ke cache untuk kemudian bisa di load ulang.
        <component :is="selectedComponent"> -> contoh penggunaan tag component yang berguna jika kita mau membuat dynamic component
                        dengan input value nama tag component/identifier. dalam tag component tersebut membutuhkan 1 parameter, yaitu is=""
                        gunanya adalah untuk mendeteksi lewat identifier pada component yang di register, component mana yang mau di render.
       -->
      <component :is="selectedComponent"></component>
    </keep-alive>
  </div>
</template>

<script>
import TheHeader from "./components/layout/TheHeader.vue";
// import BadgeList from './components/BadgeList.vue';
// import UserInfo from './components/UserInfo.vue';
// import CourseGoals from './components/CourseGoals.vue';
import ActiveGoals from "./components/ActiveGoals.vue";
import ManageGoals from "./components/ManageGoals.vue";

export default {
  components: {
    TheHeader,
    ActiveGoals,
    ManageGoals,
  },
  data() {
    return {
      selectedComponent: "active-goals",
      activeUser: {
        name: "Maximilian Schwarzmüller",
        description: "Site owner and admin",
        role: "admin",
      },
    };
  },
  methods: {
    setSelectedComponent(cmp) {
      this.selectedComponent = cmp;
    },
  },
};
</script>

<style>
html {
  font-family: sans-serif;
}

body {
  margin: 0;
}
</style>